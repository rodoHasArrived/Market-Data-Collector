<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MarketDataCollector.Uwp.Controls.DataCoverageCalendar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MarketDataCollector.Uwp.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header with navigation -->
        <Grid Grid.Row="0" Padding="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button x:Name="PrevMonthButton" Click="PrevMonth_Click" Padding="8">
                    <FontIcon Glyph="&#xE76B;" FontSize="12" />
                </Button>
                <Button x:Name="NextMonthButton" Click="NextMonth_Click" Padding="8">
                    <FontIcon Glyph="&#xE76C;" FontSize="12" />
                </Button>
                <TextBlock x:Name="MonthYearText" Text="January 2026"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           VerticalAlignment="Center" Margin="8,0,0,0" />
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                <ComboBox x:Name="SymbolFilterCombo" Header="Symbol" Width="120"
                          SelectionChanged="SymbolFilter_Changed">
                    <ComboBoxItem Content="All Symbols" Tag="*" IsSelected="True" />
                </ComboBox>
                <Button Content="Today" Click="Today_Click" VerticalAlignment="Bottom" />
            </StackPanel>
        </Grid>

        <!-- Day of week headers -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Sun" HorizontalAlignment="Center"
                       Foreground="{ThemeResource SystemBaseMediumColor}" FontSize="12" />
            <TextBlock Grid.Column="1" Text="Mon" HorizontalAlignment="Center"
                       Foreground="{ThemeResource SystemBaseMediumColor}" FontSize="12" />
            <TextBlock Grid.Column="2" Text="Tue" HorizontalAlignment="Center"
                       Foreground="{ThemeResource SystemBaseMediumColor}" FontSize="12" />
            <TextBlock Grid.Column="3" Text="Wed" HorizontalAlignment="Center"
                       Foreground="{ThemeResource SystemBaseMediumColor}" FontSize="12" />
            <TextBlock Grid.Column="4" Text="Thu" HorizontalAlignment="Center"
                       Foreground="{ThemeResource SystemBaseMediumColor}" FontSize="12" />
            <TextBlock Grid.Column="5" Text="Fri" HorizontalAlignment="Center"
                       Foreground="{ThemeResource SystemBaseMediumColor}" FontSize="12" />
            <TextBlock Grid.Column="6" Text="Sat" HorizontalAlignment="Center"
                       Foreground="{ThemeResource SystemBaseMediumColor}" FontSize="12" />
        </Grid>

        <!-- Calendar grid -->
        <Grid x:Name="CalendarGrid" Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
        </Grid>

        <!-- Legend -->
        <Border Grid.Row="3" Padding="0,16,0,0">
            <StackPanel Orientation="Horizontal" Spacing="24" HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Border Width="16" Height="16" CornerRadius="2" Background="#48BB78" />
                    <TextBlock Text="Complete" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Border Width="16" Height="16" CornerRadius="2" Background="#ED8936" />
                    <TextBlock Text="Partial" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Border Width="16" Height="16" CornerRadius="2" Background="#F56565" />
                    <TextBlock Text="Missing" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Border Width="16" Height="16" CornerRadius="2"
                            Background="{ThemeResource SystemBaseLowColor}" />
                    <TextBlock Text="Weekend/Holiday" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
