<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MarketDataCollector.Uwp.Controls.MetricCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MarketDataCollector.Uwp.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!-- Variant-specific background colors with transparency -->
        <SolidColorBrush x:Key="SuccessTrendBackground" Color="#1A3fb950" />
        <SolidColorBrush x:Key="DangerTrendBackground" Color="#1Af85149" />
        <SolidColorBrush x:Key="WarningTrendBackground" Color="#1Ad29922" />
        <SolidColorBrush x:Key="InfoTrendBackground" Color="#1A58a6ff" />
    </UserControl.Resources>

    <Border x:Name="CardBorder"
            Background="{StaticResource ConsoleBackgroundMediumBrush}"
            BorderBrush="{StaticResource ConsoleBorderBrush}"
            BorderThickness="1,1,1,3"
            CornerRadius="12"
            Padding="20"
            AutomationProperties.Name="{x:Bind Title, Mode=OneWay}"
            AutomationProperties.HelpText="{x:Bind Description, Mode=OneWay}">
        <Grid RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header: Icon and Trend Badge -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Optional Icon -->
                <FontIcon x:Name="CardIcon"
                          Grid.Column="0"
                          Glyph="{x:Bind IconGlyph, Mode=OneWay}"
                          FontSize="16"
                          Foreground="{x:Bind AccentBrush, Mode=OneWay}"
                          Visibility="{x:Bind IconVisibility, Mode=OneWay}"
                          Margin="0,0,8,0"
                          VerticalAlignment="Center" />

                <!-- Trend Badge -->
                <Border x:Name="TrendBadge"
                        Grid.Column="2"
                        CornerRadius="4"
                        Padding="8,4"
                        Visibility="{x:Bind TrendVisibility, Mode=OneWay}"
                        HorizontalAlignment="Right">
                    <TextBlock x:Name="TrendText"
                               Text="{x:Bind TrendValue, Mode=OneWay}"
                               FontSize="11"
                               FontWeight="SemiBold" />
                </Border>
            </Grid>

            <!-- Main Value -->
            <TextBlock x:Name="ValueText"
                       Grid.Row="1"
                       Text="{x:Bind Value, Mode=OneWay}"
                       FontFamily="Cascadia Code, JetBrains Mono, Consolas, monospace"
                       FontSize="{x:Bind ValueFontSize, Mode=OneWay}"
                       FontWeight="Bold"
                       AutomationProperties.LiveSetting="Polite" />

            <!-- Label -->
            <TextBlock x:Name="LabelText"
                       Grid.Row="2"
                       Text="{x:Bind Title, Mode=OneWay}"
                       FontSize="12"
                       Foreground="{StaticResource ConsoleTextMutedBrush}"
                       CharacterSpacing="50"
                       Margin="0,4,0,8" />

            <!-- Description (optional) -->
            <TextBlock x:Name="DescriptionText"
                       Grid.Row="3"
                       Text="{x:Bind Description, Mode=OneWay}"
                       FontSize="11"
                       Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                       TextWrapping="Wrap"
                       Visibility="{x:Bind DescriptionVisibility, Mode=OneWay}"
                       Margin="0,0,0,8" />

            <!-- Sparkline Area -->
            <Canvas x:Name="SparklineCanvas"
                    Grid.Row="4"
                    Height="30"
                    Background="Transparent"
                    Visibility="{x:Bind SparklineVisibility, Mode=OneWay}">
                <Polyline x:Name="SparklinePath"
                          StrokeThickness="2" />
            </Canvas>
        </Grid>
    </Border>
</UserControl>
