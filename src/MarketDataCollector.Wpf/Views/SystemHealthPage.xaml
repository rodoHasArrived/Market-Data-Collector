<Page x:Class="MarketDataCollector.Wpf.Views.SystemHealthPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource ConsoleBackgroundDarkBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel MaxWidth="1200">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                <TextBlock Text="&#xE9F5;" FontFamily="{StaticResource SymbolThemeFontFamily}"
                           FontSize="28" Foreground="{StaticResource InfoColorBrush}" VerticalAlignment="Center" />
                <TextBlock Text="System Health" Style="{StaticResource PageTitleStyle}" Margin="12,0,0,0" VerticalAlignment="Center" />
            </StackPanel>
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" /><ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" /><ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" /><ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="12%" FontSize="32" FontWeight="Bold" Foreground="{StaticResource SuccessColorBrush}" HorizontalAlignment="Center" />
                        <TextBlock Text="CPU Usage" HorizontalAlignment="Center" Foreground="{StaticResource ConsoleTextMutedBrush}" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="256 MB" FontSize="32" FontWeight="Bold" Foreground="{StaticResource InfoColorBrush}" HorizontalAlignment="Center" />
                        <TextBlock Text="Memory" HorizontalAlignment="Center" Foreground="{StaticResource ConsoleTextMutedBrush}" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="4" Style="{StaticResource CardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="24" FontSize="32" FontWeight="Bold" Foreground="{StaticResource InfoColorBrush}" HorizontalAlignment="Center" />
                        <TextBlock Text="Threads" HorizontalAlignment="Center" Foreground="{StaticResource ConsoleTextMutedBrush}" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="6" Style="{StaticResource CardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="2h 15m" FontSize="32" FontWeight="Bold" Foreground="{StaticResource SuccessColorBrush}" HorizontalAlignment="Center" />
                        <TextBlock Text="Uptime" HorizontalAlignment="Center" Foreground="{StaticResource ConsoleTextMutedBrush}" />
                    </StackPanel>
                </Border>
            </Grid>
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Health Checks" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,16" />
                    <TextBlock Text="All systems operational." Foreground="{StaticResource SuccessColorBrush}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
